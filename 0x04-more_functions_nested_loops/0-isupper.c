#include "main.h"
/**
 * _isupper - Checks if a character is an uppercase letter
 *
 * Return: 0 or 1 (Depending on the result)
 */
int _isupper(int c)
{
if(c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
