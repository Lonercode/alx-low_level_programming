#include "main.h"
/**
 * reverse_array - Reverse Array
 * @a: input 1
 * @n: input 2
 * Return: Nothing
 */
void reverse_array(int *a, int n)
{
int l;
int it;

for (l = n-1; l >=0; l--)
{
it = [l];
a[l] = it;
}
}
