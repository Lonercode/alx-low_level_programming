#include "main.h"
/**
 * _islower - Determine if lowercase
 *
 * Return: 0 or 1 (Depending on result)
 */
int _islower(int r)
{
int r;
if (r >= 'a' && r <= 'z')
{
return (1);
}
else
{
return (0);
}
}
