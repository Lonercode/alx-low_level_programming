#include "main.h"
/**
 * _isdigit - Checks if a character is a digit (1 - 9)
 *
 * Return: 0 or 1 (Depending on the result)
 */
int _isdigit(int c)
{
if (c >= 1 && c <= 9)
{
return (1);
}
else
{
return (0);
}
}
